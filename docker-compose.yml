services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: impreglon
      POSTGRES_USER: impreglon
      POSTGRES_PASSWORD: dev_password_change_me
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
